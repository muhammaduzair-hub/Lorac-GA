# Local (CPU) development + unit tests. Python 3.11.
# numpy is capped: torch 2.2.0 wheels are built against numpy 1.x and crash on 2.x.
torch==2.2.0
numpy>=1.24,<2
transformers==4.36.0
peft==0.7.0
datasets==2.16.1
pyyaml>=6.0
omegaconf>=2.3
matplotlib>=3.7
pytest>=7.4
