# Kaggle / GPU environment dependencies.
# FederatedScope installs separately via:
#   pip install -e third_party/FederatedScope
# Run that AFTER this file inside the Kaggle notebook.

transformers==4.36.0
peft==0.7.0
accelerate==0.25.0
datasets==2.16.1
numpy>=1.24,<2   # torch 2.2 wheels are built against numpy 1.x
pyyaml>=6.0
omegaconf>=2.3
matplotlib>=3.7
seaborn>=0.12
